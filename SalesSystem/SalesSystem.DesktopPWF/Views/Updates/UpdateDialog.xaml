<Window x:Class="SalesSystem.DesktopPWF.Views.Updates.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تحديث البرنامج"
        Width="520" Height="Auto"
        MinHeight="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        FlowDirection="RightToLeft"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Background" Value="#0078D4"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#888888"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GhostButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="36"/>
            <Setter Property="Padding" Value="16,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#555555"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DangerGhostButtonStyle" TargetType="Button" BasedOn="{StaticResource GhostButtonStyle}">
            <Setter Property="Foreground" Value="#F44336"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FFEBEE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="ChangelogItemStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Margin" Value="0,3"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
    </Window.Resources>

    <Border CornerRadius="12"
            Background="White"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect BlurRadius="20" Opacity="0.2"
                              ShadowDepth="4" Color="Black"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="#0078D4"
                    CornerRadius="12,12,0,0"
                    Padding="24,20">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="تحديث متوفر"
                                   Foreground="#BBDEFB"
                                   FontSize="11"
                                   Margin="0,0,0,4"/>

                        <TextBlock Text="{Binding HeaderText}"
                                   Foreground="White"
                                   FontSize="16"
                                   FontWeight="Bold"
                                   TextWrapping="Wrap"/>

                        <TextBlock Text="{Binding SubHeaderText}"
                                   Foreground="#90CAF9"
                                   FontSize="11"
                                   Margin="0,6,0,0"/>
                    </StackPanel>

                    <Border Grid.Column="1"
                            Background="#FF5722"
                            CornerRadius="4"
                            Padding="8,4"
                            VerticalAlignment="Top"
                            Visibility="{Binding IsCriticalUpdate,
                                         Converter={StaticResource BoolToVisibility}}">
                        <TextBlock Text="تحديث إلزامي"
                                   Foreground="White"
                                   FontSize="10"
                                   FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>

            <Grid Grid.Row="1" Margin="24,16,24,8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Border Background="#F5F5F5" CornerRadius="6" Padding="12,8">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="الإصدار الحالي"
                                   FontSize="10" Foreground="#888"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding CurrentVersion}"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="#555"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <TextBlock Grid.Column="1"
                           Text="  ←  "
                           FontSize="20" Foreground="#0078D4"
                           VerticalAlignment="Center"/>

                <Border Grid.Column="2"
                        Background="#E3F2FD"
                        BorderBrush="#0078D4"
                        BorderThickness="2"
                        CornerRadius="6" Padding="12,8">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="الإصدار الجديد"
                                   FontSize="10" Foreground="#0078D4"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding LatestVersion}"
                                   FontSize="14" FontWeight="Bold"
                                   Foreground="#0078D4"
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="{Binding ReleaseDate}"
                                   FontSize="9" Foreground="#888"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </Grid>

            <Border Grid.Row="2"
                    Margin="24,0,24,12"
                    BorderBrush="#EEEEEE"
                    BorderThickness="1"
                    CornerRadius="6">
                <StackPanel>
                    <TextBlock Text="ما الجديد في هذا الإصدار:"
                               FontSize="11" FontWeight="Bold"
                               Foreground="#444"
                               Margin="12,10,12,6"/>

                    <ScrollViewer MaxHeight="160"
                                  VerticalScrollBarVisibility="Auto"
                                  Margin="12,0,12,10">
                        <ItemsControl ItemsSource="{Binding ChangelogItems}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding}"
                                               Style="{StaticResource ChangelogItemStyle}"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

            <StackPanel Grid.Row="3"
                        Margin="24,0,24,12"
                        Visibility="{Binding IsDownloading,
                                     Converter={StaticResource BoolToVisibility}}">

                <ProgressBar Value="{Binding DownloadProgress}"
                             Minimum="0" Maximum="100"
                             Height="8"
                             Background="#E0E0E0"
                             Foreground="#0078D4"/>

                <TextBlock Text="{Binding DownloadStatusText}"
                           FontSize="11" Foreground="#666"
                           HorizontalAlignment="Center"
                           Margin="0,6,0,0"/>
            </StackPanel>

            <Border Grid.Row="4"
                    BorderBrush="#EEEEEE"
                    BorderThickness="0,1,0,0"
                    Padding="24,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button Grid.Column="0"
                            Content="تخطي هذا الإصدار"
                            Style="{StaticResource GhostButtonStyle}"
                            Command="{Binding SkipVersionCommand}"
                            ToolTip="تجاهل هذا الإصدار وعدم التذكير به مرة أخرى"
                            Visibility="{Binding CanSkip,
                                         Converter={StaticResource BoolToVisibility}}"/>

                    <Button Grid.Column="1"
                            Content="إلغاء التحميل"
                            Style="{StaticResource DangerGhostButtonStyle}"
                            Command="{Binding CancelDownloadCommand}"
                            ToolTip="إيقاف عملية التحميل الجاري"
                            Visibility="{Binding IsDownloading,
                                         Converter={StaticResource BoolToVisibility}}"/>

                    <StackPanel Grid.Column="2"/>

                    <StackPanel Grid.Column="3"
                                Orientation="Horizontal">

                        <Button Content="تذكيري لاحقاً"
                                Style="{StaticResource GhostButtonStyle}"
                                Command="{Binding RemindLaterCommand}"
                                ToolTip="إغلاق النافذة والتذكير بالتحديث لاحقاً"
                                Margin="0,0,8,0"
                                Visibility="{Binding CanSkip,
                                             Converter={StaticResource BoolToVisibility}}"/>

                        <Button Content="{Binding DownloadButtonText}"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding DownloadAndInstallCommand}"
                                ToolTip="بدء تحميل التحديث وتثبيته تلقائياً بعد اكتمال التحميل"
                                MinWidth="160"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>
