<UserControl x:Class="SalesSystem.DesktopPWF.Views.Permissions.PermissionManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             FlowDirection="RightToLeft"
             Background="{StaticResource BackgroundBrush}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="🔐  إدارة الصلاحيات"
                   Style="{StaticResource HeaderStyle}"
                   AutomationProperties.AutomationId="PermissionPageTitle"/>

        <!-- Role Selector -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Padding="12" Margin="0,0,0,8">
            <StackPanel>
                <TextBlock Text="اختر الدور" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
                <StackPanel Orientation="Horizontal">
                    <RadioButton Content="مدير النظام"
                                 GroupName="RoleSelector"
                                 IsChecked="{Binding IsAdminSelected}"
                                 Margin="0,0,20,0"
                                 ToolTip="صلاحية كاملة — جميع الصلاحيات متاحة لمدير النظام"/>
                    <RadioButton Content="مدير فرع"
                                 GroupName="RoleSelector"
                                 IsChecked="{Binding IsManagerSelected}"
                                 Margin="0,0,20,0"
                                 ToolTip="صلاحية مشرف — المبيعات والمشتريات والمنتجات والتقارير"/>
                    <RadioButton Content="كاشير"
                                 GroupName="RoleSelector"
                                 IsChecked="{Binding IsCashierSelected}"
                                 ToolTip="صلاحية محدودة — المبيعات ومرتجعات المبيعات فقط"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Action Bar -->
        <Border Grid.Row="2" Style="{StaticResource ToolbarStyle}" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding SelectAllCommand}"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="100"
                        Margin="0,0,6,0"
                        ToolTip="تحديد جميع الصلاحيات للدور الحالي">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✅" Margin="0,0,6,0"/>
                        <TextBlock Text="تحديد الكل"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DeselectAllCommand}"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="100"
                        ToolTip="إلغاء تحديد جميع الصلاحيات للدور الحالي">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❌" Margin="0,0,6,0"/>
                        <TextBlock Text="إلغاء الكل"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Error Banner -->
        <Border Grid.Row="3"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}"
                Background="{StaticResource ErrorLightBrush}"
                BorderBrush="{StaticResource ErrorBrush}"
                BorderThickness="1" CornerRadius="8" Padding="12"
                Margin="0,0,0,8"
                VerticalAlignment="Top">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="⚠️" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                <TextBlock Grid.Column="1" Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorTextBrush}" VerticalAlignment="Center" TextWrapping="Wrap"/>
            </Grid>
        </Border>

        <!-- Permissions List - Scrollable with Expanders per Category -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Padding="8" Margin="0,0,0,6">
                            <Expander Header="{Binding CategoryName}" IsExpanded="True"
                                      Style="{StaticResource SidebarExpanderStyle}"
                                      ToolTip="الصلاحيات المتاحة ضمن هذا التصنيف">
                                <ItemsControl ItemsSource="{Binding Permissions}" Margin="8,4,0,4">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsChecked}"
                                                      Margin="0,2"
                                                      ToolTip="{Binding DisplayNameAr}">
                                                <TextBlock Text="{Binding DisplayNameAr}"
                                                           FontSize="13"
                                                           Margin="4,0,0,0"/>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="4" Background="White" Padding="12,8" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButton}"
                        MinWidth="140"
                        IsDefault="True"
                        ToolTip="حفظ صلاحيات الدور المحدد">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconSave}" Fill="White" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                        <TextBlock Text="حفظ الصلاحيات"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="5"
                Background="#80FFFFFF"
                Panel.ZIndex="1000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Background="#E3E8EE" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="جاري تحميل الصلاحيات..." FontSize="14" Margin="0,12,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
