<UserControl x:Class="SalesSystem.DesktopPWF.Views.Purchases.PurchaseOrderEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SalesSystem.DesktopPWF.ViewModels.Purchases"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="900"
             d:DataContext="{d:DesignInstance local:PurchaseOrderEditorViewModel, IsDesignTimeCreatable=False}">

    <Grid>
        <!-- Loading Overlay -->
        <Border Background="#80FFFFFF" Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}"
                Grid.ZIndex="10" IsHitTestVisible="True">
            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Margin="0,0,0,8"/>
                <TextBlock Text="جاري التحميل..." FontSize="14" Foreground="#666"
                           HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>

        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="12">
                <!-- Header -->
                <Border Background="{StaticResource PrimaryBrush}" Padding="12,6"
                        CornerRadius="4" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock x:Name="HeaderTitle" FontSize="14" FontWeight="Bold"
                                   Foreground="White" VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <!-- Error Message -->
                <Border Background="#FFF3F3" BorderBrush="#EF4444" BorderThickness="1"
                        CornerRadius="4" Padding="8,6" Margin="0,0,0,8"
                        Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}">
                    <Grid>
                        <TextBlock Text="{Binding ErrorMessage}" Foreground="#B91C1C"
                                   FontSize="11" VerticalAlignment="Center"/>
                        <Button Content="✕" FontSize="11" Foreground="#B91C1C"
                                Background="Transparent" BorderThickness="0"
                                HorizontalAlignment="Left" Cursor="Hand"
                                ToolTip="إخفاء رسالة الخطأ"
                                Command="{Binding DismissErrorCommand}"/>
                    </Grid>
                </Border>

                <!-- Main Form Sections -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="12"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Right Column: Supplier + Warehouse + Dates -->
                    <StackPanel Grid.Column="0">
                        <!-- Supplier Section -->
                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                                Padding="0,0,0,6" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="بيانات المورد" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="المورد *" Style="{StaticResource LabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Suppliers}"
                                          SelectedValue="{Binding SelectedSupplierId}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="Name"
                                          Style="{StaticResource ModernComboBox}"
                                          Margin="0,0,0,6"
                                          ToolTip="اختر المورد الذي تريد الشراء منه — هذا الحقل إلزامي"/>
                                <TextBlock Text="المستودع *" Style="{StaticResource LabelStyle}"/>
                                <ComboBox ItemsSource="{Binding Warehouses}"
                                          SelectedValue="{Binding SelectedWarehouseId}"
                                          SelectedValuePath="Id"
                                          DisplayMemberPath="Name"
                                          Style="{StaticResource ModernComboBox}"
                                          Margin="0,0,0,6"
                                          ToolTip="اختر المستودع الذي ستستلم البضاعة إليه — هذا الحقل إلزامي"/>
                            </StackPanel>
                        </Border>

                        <!-- Dates Section -->
                        <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,0,1"
                                Padding="0,0,0,6" Margin="0,0,0,8">
                            <StackPanel>
                                <TextBlock Text="التواريخ" FontSize="13" FontWeight="SemiBold"
                                           Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,6"/>
                                <TextBlock Text="تاريخ الطلب" Style="{StaticResource LabelStyle}"/>
                                <DatePicker SelectedDate="{Binding OrderDate}"
                                            Style="{StaticResource ModernDatePicker}"
                                            Margin="0,0,0,6"
                                            ToolTip="تاريخ إصدار أمر الشراء"/>
                                <TextBlock Text="تاريخ التوقع" Style="{StaticResource LabelStyle}"/>
                                <DatePicker SelectedDate="{Binding ExpectedDate.Value}"
                                            Style="{StaticResource ModernDatePicker}"
                                            Margin="0,0,0,6"
                                            ToolTip="التاريخ المتوقع لاستلام البضاعة — اختياري"/>
                            </StackPanel>
                        </Border>

                        <!-- Currency Section -->
                        <StackPanel>
                            <TextBlock Text="العملة" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,6"/>
                            <TextBlock Text="العملة" Style="{StaticResource LabelStyle}"/>
                            <ComboBox ItemsSource="{Binding Currencies}"
                                      SelectedValue="{Binding SelectedCurrencyId}"
                                      SelectedValuePath="Id"
                                      DisplayMemberPath="Name"
                                      Style="{StaticResource ModernComboBox}"
                                      Margin="0,0,0,6"
                                      ToolTip="اختر عملة الأمر — إذا كانت غير العملة الأساسية سيظهر حقل سعر الصرف"/>
                            <TextBlock Text="سعر الصرف" Style="{StaticResource LabelStyle}"
                                       Visibility="{Binding IsForeignCurrency, Converter={StaticResource BoolToVisibility}}"/>
                            <TextBox Text="{Binding ExchangeRate, StringFormat=N2}"
                                     Style="{StaticResource ModernTextBox}"
                                     Margin="0,0,0,6"
                                     ToolTip="سعر صرف العملة المختارة مقابل العملة الأساسية — إلزامي للعملات الأجنبية"
                                     Visibility="{Binding IsForeignCurrency, Converter={StaticResource BoolToVisibility}}"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Left Column: Items -->
                    <StackPanel Grid.Column="2">
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="الأصناف" FontSize="13" FontWeight="SemiBold"
                                       Foreground="{StaticResource PrimaryBrush}"
                                       VerticalAlignment="Center"/>
                            <Button Command="{Binding AddItemCommand}"
                                    Content="+ إضافة صنف"
                                    Style="{StaticResource SmallPrimaryButton}"
                                    Margin="8,0,0,0"
                                    ToolTip="إضافة صنف جديد إلى قائمة أصناف الأمر"/>
                        </StackPanel>

                        <ItemsControl ItemsSource="{Binding Items}"
                                      Margin="0,0,0,8">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate DataType="{x:Type local:PurchaseOrderItemLineViewModel}">
                                    <Border BorderBrush="{StaticResource BorderBrush}"
                                            BorderThickness="1" CornerRadius="4"
                                            Padding="6,4" Margin="0,0,0,4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="100"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Product -->
                                            <StackPanel Grid.Column="0" Margin="0,0,4,0">
                                                <TextBlock Text="المنتج *" Style="{StaticResource LabelStyle}" FontSize="10"/>
                                                <ComboBox ItemsSource="{Binding AvailableProducts}"
                                                          SelectedItem="{Binding SelectedProduct}"
                                                          DisplayMemberPath="Name"
                                                          Style="{StaticResource ModernComboBox}"
                                                          Height="24"
                                                          ToolTip="اختر المنتج لهذا البند — اكتب للبحث"/>
                                            </StackPanel>

                                            <!-- Quantity -->
                                            <StackPanel Grid.Column="1" Margin="0,0,4,0">
                                                <TextBlock Text="الكمية *" Style="{StaticResource LabelStyle}" FontSize="10"/>
                                                <TextBox Text="{Binding Quantity, StringFormat=N3}"
                                                         Style="{StaticResource ModernTextBox}"
                                                         Height="24"
                                                         ToolTip="أدخل الكمية — يجب أن تكون أكبر من صفر"/>
                                            </StackPanel>

                                            <!-- Unit Cost -->
                                            <StackPanel Grid.Column="2" Margin="0,0,4,0">
                                                <TextBlock Text="التكلفة" Style="{StaticResource LabelStyle}" FontSize="10"/>
                                                <TextBox Text="{Binding UnitCost, StringFormat=N2}"
                                                         Style="{StaticResource ModernTextBox}"
                                                         Height="24"
                                                         ToolTip="تكلفة الوحدة من المنتج"/>
                                            </StackPanel>

                                            <!-- Line Total -->
                                            <StackPanel Grid.Column="3" Margin="0,0,4,0">
                                                <TextBlock Text="الإجمالي" Style="{StaticResource LabelStyle}" FontSize="10"/>
                                                <TextBlock Text="{Binding LineTotal, StringFormat=N2}"
                                                           FontWeight="Bold" FontSize="12"
                                                           VerticalAlignment="Center" Margin="0,2,0,0"
                                                           ToolTip="إجمالي قيمة البند = الكمية × التكلفة"/>
                                            </StackPanel>

                                            <!-- Remove Button -->
                                            <Button Grid.Column="4"
                                                    Command="{Binding DataContext.RemoveItemCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                    CommandParameter="{Binding}"
                                                    Content="✕"
                                                    Width="24" Height="24"
                                                    FontSize="11"
                                                    Margin="2,8,0,0"
                                                    VerticalAlignment="Top"
                                                    ToolTip="حذف هذا الصنف من الأمر">
                                                <Button.Style>
                                                    <Style TargetType="Button">
                                                        <Setter Property="Background" Value="Transparent"/>
                                                        <Setter Property="BorderThickness" Value="0"/>
                                                        <Setter Property="Foreground" Value="#EF4444"/>
                                                        <Setter Property="Cursor" Value="Hand"/>
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Foreground" Value="#DC2626"/>
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Button.Style>
                                            </Button>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Grid>

                <!-- Notes -->
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                        Padding="0,8,0,0" Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="ملاحظات" FontSize="13" FontWeight="SemiBold"
                                   Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,6"/>
                        <TextBox Text="{Binding Notes}"
                                 Style="{StaticResource ModernTextBox}"
                                 TextWrapping="Wrap" AcceptsReturn="True"
                                 MinHeight="60" MaxHeight="120"
                                 ToolTip="ملاحظات إضافية حول أمر الشراء — اختياري"/>
                    </StackPanel>
                </Border>

                <!-- Totals -->
                <Border BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,1,0,0"
                        Padding="0,8,0,0" Margin="0,8,0,0">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="1" HorizontalAlignment="Right">
                            <Grid Margin="0,0,0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="المجموع الفرعي:" FontWeight="SemiBold"
                                           HorizontalAlignment="Left" FontSize="12"/>
                                <TextBlock Grid.Column="1" Text="{Binding SubTotal, StringFormat=N2}"
                                           FontWeight="Bold" FontSize="12"
                                           HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="الإجمالي:" FontWeight="Bold" FontSize="14"
                                           HorizontalAlignment="Left"
                                           Foreground="{StaticResource PrimaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="{Binding TotalAmount, StringFormat=N2}"
                                           FontWeight="Bold" FontSize="14"
                                           HorizontalAlignment="Right"
                                           Foreground="{StaticResource PrimaryBrush}"/>
                            </Grid>
                        </StackPanel>
                    </Grid>
                </Border>

                <!-- Buttons -->
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"
                            Margin="0,16,0,0">
                    <Button Command="{Binding SaveCommand}"
                            Style="{StaticResource PrimaryButton}"
                            ToolTip="حفظ أمر الشراء — سيتم التحقق من صحة البيانات قبل الحفظ">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" Margin="0,0,4,0"/>
                            <TextBlock Text="حفظ الأمر"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding CancelCommand}"
                            Style="{StaticResource SecondaryButton}"
                            Margin="8,0,0,0"
                            ToolTip="إلغاء وإغلاق نافذة تحرير الأمر">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🚫" Margin="0,0,4,0"/>
                            <TextBlock Text="إلغاء"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
