<Window x:Class="SalesSystem.DesktopPWF.Views.Dialogs.SuccessDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="نجاح"
        Height="200" Width="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        Background="#F5F5F5">

    <Border Margin="15" Background="White" CornerRadius="8" Padding="20">
        <Border.Effect>
            <DropShadowEffect BlurRadius="15" ShadowDepth="3" Opacity="0.3"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
                <Border Background="#E8F5E9" CornerRadius="25" Width="50" Height="50">
                    <TextBlock Text="&#10004;" FontSize="24" FontWeight="Bold" Foreground="#388E3C"
                               HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="نجاح" FontSize="20" FontWeight="Bold" Foreground="#388E3C"
                           VerticalAlignment="Center" Margin="12,0,0,0"/>
            </StackPanel>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="14"
                           Foreground="#333" LineHeight="24"/>
            </ScrollViewer>

            <Button Grid.Row="2" Content="حسناً" Width="100" Height="35"
                    HorizontalAlignment="Center" Margin="0,12,0,0"
                    Background="#388E3C" Foreground="White" FontWeight="SemiBold"
                    BorderThickness="0" Cursor="Hand" Click="CloseButton_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6" Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>