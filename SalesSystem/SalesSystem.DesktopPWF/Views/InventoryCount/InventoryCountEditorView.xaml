<UserControl x:Class="SalesSystem.DesktopPWF.Views.InventoryCount.InventoryCountEditorView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:helpers="clr-namespace:SalesSystem.DesktopPWF.Helpers"
      mc:Ignorable="d"
      FlowDirection="RightToLeft"
      Background="{StaticResource BackgroundBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="12,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="📋" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                <TextBlock Text="{Binding Title}" Style="{StaticResource HeaderStyle}" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="16">
            <StackPanel>
                <Border Style="{StaticResource CardStyle}" Padding="16">
                    <StackPanel>
                        <!-- Count No -->
                        <TextBlock Text="رقم الجرد" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding CountNo, Mode=OneWay}" Style="{StaticResource ModernTextBox}"
                                 IsReadOnly="True" Tag="يُحدد تلقائياً"
                                 ToolTip="رقم الجرد — يُحدد تلقائياً بواسطة النظام"/>
                        <Border Height="12"/>

                        <!-- Warehouse + Date -->
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0" Margin="0,0,8,0">
                                <TextBlock Text="رقم المستودع *" Style="{StaticResource LabelStyle}"/>
                                <TextBox Text="{Binding WarehouseId, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                                         Style="{StaticResource ModernTextBox}" Tag="أدخل رقم المستودع"
                                         ToolTip="أدخل رقم المستودع — هذا الحقل إلزامي"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1" Margin="8,0,0,0">
                                <TextBlock Text="تاريخ الجرد *" Style="{StaticResource LabelStyle}"/>
                                <DatePicker SelectedDate="{Binding CountDate}" Style="{StaticResource ModernDatePicker}"
                                            ToolTip="تاريخ الجرد"/>
                            </StackPanel>
                        </Grid>
                        <Border Height="12"/>

                        <!-- Notes -->
                        <TextBlock Text="ملاحظات" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource ModernTextBox}"
                                 Tag="ملاحظات" TextWrapping="Wrap" AcceptsReturn="True" Height="60"
                                 ToolTip="ملاحظات حول الجرد"/>
                        <Border Height="16"/>

                        <!-- Lines Section -->
                        <TextBlock Text="بنود الجرد" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" Margin="0,0,0,8"/>
                        <Border Background="{StaticResource InfoLightBrush}" Padding="12,8" CornerRadius="6" Margin="0,0,0,12">
                            <TextBlock Text="الكمية الفعلية: أدخل الكمية الفعلية الموجودة في المستودع. سيتم حساب الفرق تلقائياً."
                                       FontSize="11" Foreground="{StaticResource InfoTextBrush}" TextWrapping="Wrap"/>
                        </Border>

                        <DataGrid ItemsSource="{Binding Lines}" SelectedItem="{Binding SelectedLine}"
                                  Style="{StaticResource ModernDataGrid}" IsReadOnly="True" Height="200"
                                  AutoGenerateColumns="False">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="المنتج" Binding="{Binding ProductName}" Width="*"/>
                                <DataGridTextColumn Header="الكمية النظامية" Binding="{Binding SystemQuantity, StringFormat=N3}" Width="100" CellStyle="{StaticResource DataGridCellCenterStyle}"/>
                                <DataGridTextColumn Header="الكمية الفعلية" Binding="{Binding ActualQuantity, StringFormat=N3}" Width="100" CellStyle="{StaticResource DataGridCellCenterStyle}"/>
                                <DataGridTextColumn Header="الفرق" Binding="{Binding Difference, StringFormat=N3}" Width="100" CellStyle="{StaticResource DataGridCellCenterStyle}"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="White" Padding="12,8" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Command="{Binding SaveCommand}" Style="{StaticResource PrimaryButton}" Width="140" Margin="0,0,12,0" IsDefault="True"
                            ToolTip="حفظ الجرد — سيتم التحقق من صحة الحقول قبل الحفظ">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="💾" Margin="0,0,8,0" VerticalAlignment="Center"/>
                            <TextBlock Text="حفظ"/>
                        </StackPanel>
                    </Button>
                    <Button Content="إلغاء" Command="{Binding CancelDialogCommand}" Style="{StaticResource SecondaryButton}" Width="100"
                            ToolTip="إلغاء وإغلاق النافذة"/>
                </StackPanel>

                <TextBlock Grid.Row="1" Text="{Binding ErrorMessage}" Foreground="{StaticResource ErrorBrush}"
                           FontWeight="SemiBold" HorizontalAlignment="Center" TextWrapping="Wrap"
                           Margin="0,8,0,0" Visibility="{Binding ErrorMessage, Converter={StaticResource StringToVisibility}}"/>
            </Grid>
        </Border>

        <!-- Loading -->
        <Border Grid.Row="0" Grid.RowSpan="3" Background="#80FFFFFF" Panel.ZIndex="1000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Background="#E3E8EE" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="جاري الحفظ..." FontSize="14" Margin="0,12,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
