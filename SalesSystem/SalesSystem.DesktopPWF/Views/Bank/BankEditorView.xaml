<UserControl x:Class="SalesSystem.DesktopPWF.Views.Bank.BankEditorView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:helpers="clr-namespace:SalesSystem.DesktopPWF.Helpers"
        mc:Ignorable="d"
        Background="{StaticResource BackgroundBrush}"
        FlowDirection="RightToLeft">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryBrush}" Padding="12,6">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Title}" Style="{StaticResource HeaderStyle}" Foreground="White" VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Main Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Padding="16">
                <StackPanel>
                    <!-- Name -->
                    <TextBlock Text="اسم البنك *" Style="{StaticResource LabelStyle}" Margin="4,0,0,8"/>
                    <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                             Style="{StaticResource ModernTextBox}"
                             Tag="أدخل اسم البنك"
                             TabIndex="1"
                             helpers:FocusHelper.AdvanceOnEnter="True"
                             ToolTip="أدخل اسم البنك — هذا الحقل إلزامي"/>

                    <!-- Account (linked account info) -->
                    <TextBlock Text="الحساب المرتبط" Style="{StaticResource LabelStyle}" Margin="4,12,0,8"/>
                    <TextBox Text="{Binding AccountId, TargetNullValue='سيتم إنشاؤه تلقائياً'}"
                             Style="{StaticResource ModernTextBox}"
                             Tag="معرف الحساب المحاسبي"
                             TabIndex="2"
                             IsReadOnly="True"
                             ToolTip="الحساب المحاسبي المرتبط — يتم إنشاؤه تلقائياً تحت الحساب 1120 (البنوك) إذا لم يتم تحديده"/>

                    <!-- Account Number -->
                    <TextBlock Text="رقم الحساب" Style="{StaticResource LabelStyle}" Margin="4,12,0,8"/>
                    <TextBox Text="{Binding AccountNumber, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}"
                             Tag="رقم الحساب البنكي"
                             TabIndex="3"
                             ToolTip="رقم الحساب البنكي (اختياري)"/>

                    <!-- IBAN -->
                    <TextBlock Text="الآيبان (IBAN)" Style="{StaticResource LabelStyle}" Margin="4,12,0,8"/>
                    <TextBox Text="{Binding Iban, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ModernTextBox}"
                             Tag="الرقم الدولي للحساب (IBAN)"
                             TabIndex="4"
                             ToolTip="الرقم الدولي للحساب المصرفي — IBAN (اختياري)"/>

                    <!-- IsActive -->
                    <CheckBox Content="نشط" 
                              IsChecked="{Binding IsActive}"
                              Margin="4,16,0,0"
                              TabIndex="5"
                              ToolTip="تفعيل أو إيقاف البنك"/>
                </StackPanel>
            </Border>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="2" Background="#F8FAFC" Padding="12,8" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Width="120"
                        IsDefault="True"
                        TabIndex="6"
                        Margin="0,0,12,0"
                        ToolTip="حفظ بيانات البنك">
                    <TextBlock Text="حفظ" VerticalAlignment="Center"/>
                </Button>
                <Button Content="إلغاء"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Width="100"
                        TabIndex="7"
                        ToolTip="إلغاء والعودة"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="3"
                Background="#CCFFFFFF"
                Panel.ZIndex="1000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="180" Height="4" Background="#E2E8F0" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Text="جاري المعالجة..." FontSize="14" FontWeight="SemiBold" Margin="0,16,0,0" Foreground="{StaticResource PrimaryBrush}" HorizontalAlignment="Center"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
