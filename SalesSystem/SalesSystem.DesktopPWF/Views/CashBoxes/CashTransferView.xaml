<Page x:Class="SalesSystem.DesktopPWF.Views.CashBoxes.CashTransferView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
      xmlns:vm="clr-namespace:SalesSystem.DesktopPWF.ViewModels.CashBoxes"
      mc:Ignorable="d"
      Title="تحويل بين الخزائن"
      FlowDirection="RightToLeft"
      Background="{StaticResource BackgroundBrush}">

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadCommand}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Style="{StaticResource PageRootGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="تحويل بين الخزائن"
                   Style="{StaticResource HeaderStyle}"
                   AutomationProperties.AutomationId="CashTransferPageTitle"/>

        <!-- Error Message Bar -->
        <Border Grid.Row="1"
                Background="{StaticResource ErrorLightBrush}"
                BorderBrush="{StaticResource ErrorBrush}"
                BorderThickness="1"
                Padding="12,8"
                Margin="{StaticResource StandardSectionMargin}"
                CornerRadius="6"
                Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibility}}">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="⚠️" Margin="0,0,10,0" VerticalAlignment="Center" FontSize="16"/>
                <TextBlock Text="{Binding ErrorMessage}"
                           Foreground="{StaticResource ErrorTextBrush}"
                           FontWeight="SemiBold"
                           VerticalAlignment="Center"
                           FontSize="12"
                           TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <!-- Form Card -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="16">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">

                    <!-- Source CashBox -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="الخزنة المصدر *" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding SourceCashBoxes}"
                                  SelectedItem="{Binding SelectedSourceCashBox}"
                                  DisplayMemberPath="BoxName"
                                  Style="{StaticResource ComboBoxStyle}"
                                  ToolTip="اختر الخزنة التي سيتم التحويل منها"/>
                        <TextBlock Text="{Binding SelectedSourceCashBox.CurrentBalance, StringFormat='الرصيد المتوفر للخزنة: {0:N2}'}"
                                   Style="{StaticResource HelperTextStyle}"
                                   Visibility="{Binding SelectedSourceCashBox, Converter={StaticResource NullToVisibility}}"/>
                    </StackPanel>

                    <!-- Destination CashBox -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="الخزنة الهدف *" Style="{StaticResource LabelStyle}"/>
                        <ComboBox ItemsSource="{Binding DestinationCashBoxes}"
                                  SelectedItem="{Binding SelectedDestinationCashBox}"
                                  DisplayMemberPath="BoxName"
                                  Style="{StaticResource ComboBoxStyle}"
                                  ToolTip="اختر الخزنة التي سيتم التحويل إليها"/>
                    </StackPanel>

                    <!-- Amount -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="المبلغ *" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBox}"
                                 ToolTip="مبلغ التحويل — يجب أن يكون أكبر من صفر"/>
                    </StackPanel>

                    <!-- Notes -->
                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="ملاحظات" Style="{StaticResource LabelStyle}"/>
                        <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}"
                                 Style="{StaticResource ModernTextBox}"
                                 ToolTip="ملاحظات إضافية حول التحويل (اختياري)"
                                 AcceptsReturn="True"
                                 Height="80"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Footer Buttons -->
        <Border Grid.Row="3" Background="White" Padding="16,8" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Command="{Binding TransferCommand}"
                        Style="{StaticResource PrimaryButton}"
                        Height="36" Padding="24,0"
                        ToolTip="تنفيذ التحويل المالي بين الخزائن — سيتم خصم المبلغ من المصدر وإضافته للهدف">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="💸" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="تحويل" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>

                <Button Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButton}"
                        Margin="8,0,0,0" Height="36" Padding="24,0"
                        ToolTip="إلغاء عملية التحويل والعودة">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❌" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="إلغاء" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="4"
                Background="#80FFFFFF"
                Panel.ZIndex="1000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Background="#E3E8EE" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="جاري تنفيذ التحويل..." FontSize="14" Margin="0,12,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Page>
