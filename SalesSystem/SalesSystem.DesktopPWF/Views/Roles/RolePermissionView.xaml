<Window x:Class="SalesSystem.DesktopPWF.Views.Roles.RolePermissionView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}"
        Height="600" Width="800"
        MinHeight="400" MinWidth="600"
        Background="{StaticResource BackgroundBrush}"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="12,6" Margin="0,0,0,8">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                    <GradientStop Color="#0F172A" Offset="0"/>
                    <GradientStop Color="#1E293B" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding WindowTitle}" Style="{StaticResource HeaderStyle}" Foreground="White"/>
            </StackPanel>
        </Border>

        <!-- Action Bar -->
        <Border Grid.Row="1" Style="{StaticResource ToolbarStyle}" Padding="8,4">
            <StackPanel Orientation="Horizontal">
                <Button Command="{Binding SelectAllCommand}"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="100"
                        Margin="0,0,6,0"
                        ToolTip="تحديد جميع الصلاحيات للدور الحالي">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="✅" Margin="0,0,6,0"/>
                        <TextBlock Text="تحديد الكل"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding DeselectAllCommand}"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="100"
                        ToolTip="إلغاء تحديد جميع الصلاحيات للدور الحالي">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❌" Margin="0,0,6,0"/>
                        <TextBlock Text="إلغاء الكل"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </Border>

        <!-- Permissions List -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto" Margin="0,4,0,0">
            <ItemsControl ItemsSource="{Binding Categories}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource CardStyle}" Padding="8" Margin="0,0,0,6">
                            <Expander Header="{Binding CategoryName}" IsExpanded="True"
                                      Style="{StaticResource SidebarExpanderStyle}"
                                      ToolTip="الصلاحيات المتاحة ضمن هذا التصنيف">
                                <ItemsControl ItemsSource="{Binding Permissions}" Margin="8,4,0,4">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <CheckBox IsChecked="{Binding IsChecked}"
                                                      Margin="0,2"
                                                      ToolTip="{Binding DisplayNameAr}">
                                                <TextBlock Text="{Binding DisplayNameAr}"
                                                           FontSize="13"
                                                           Margin="4,0,0,0"/>
                                            </CheckBox>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Expander>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- Footer -->
        <Border Grid.Row="3" Background="White" Padding="12,8" BorderThickness="0,1,0,0" BorderBrush="{StaticResource BorderBrush}">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding SaveCommand}"
                        Style="{StaticResource PrimaryButton}"
                        MinWidth="140"
                        IsDefault="True"
                        ToolTip="حفظ صلاحيات الدور المحدد">
                    <StackPanel Orientation="Horizontal">
                        <Path Data="{StaticResource IconSave}" Fill="White" Width="16" Height="16" Margin="0,0,8,0" Stretch="Uniform"/>
                        <TextBlock Text="حفظ الصلاحيات"/>
                    </StackPanel>
                </Button>
                <Button Content="إلغاء"
                        Command="{Binding CancelCommand}"
                        Style="{StaticResource SecondaryButton}"
                        MinWidth="100"
                        Margin="8,0,0,0"
                        ToolTip="إلغاء وإغلاق النافذة"/>
            </StackPanel>
        </Border>

        <!-- Loading Overlay -->
        <Border Grid.Row="0" Grid.RowSpan="4"
                Background="#80FFFFFF"
                Panel.ZIndex="1000"
                Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibility}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4" Background="#E3E8EE" Foreground="{StaticResource PrimaryBrush}"/>
                <TextBlock Text="جاري تحميل الصلاحيات..." FontSize="14" Margin="0,12,0,0" Foreground="{StaticResource TextSecondaryBrush}"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>
